{
    "name": "quickstart",
    "build": {
        "dockerfile": "Dockerfile"
    },

    "customizations": {
        "vscode": {
            "settings": {},
            "extensions": []
        }
    },

    "postAttachCommand": "bash .devcontainer/setup.sh"
}