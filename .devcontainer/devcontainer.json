{
    "name": "quickstart",
    "build": {
        "dockerfile": "Dockerfile"
    },
    "runArgs": [
        "--platform=linux/amd64"
    ],
    "customizations": {
        "vscode": {
            "settings": {},
            "extensions": [
                "ms-vscode.cpptools"
            ]
        }
    },

    "postAttachCommand": "bash .devcontainer/setup.sh"
}