{
    "name": "quickstart",
    "build": {
        "dockerfile": "Dockerfile"
    },

    "customizations": {
        "vscode": {
            "settings": {},
            "extensions": [
                "ms-vscode.cpptools"
            ]
        }
    },

    // "postAttachCommand": "bash .devcontainer/setup.sh"
}